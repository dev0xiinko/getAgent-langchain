# Base URL of the GetAgent backend (the Express server in ../). The frontend
# fetches this directly; the backend has CORS enabled for all origins.
# Copy to .env.local and adjust to wherever the agent server is listening.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
